trigger:
- master
pool:
  vmImage: 'windows-latest'
steps:
- script: |
- task: SynopsysPolaris@1
  inputs:
    polarisService: 'POLARIS_SIPSE_SERVER_URL'
    polarisCommand: '-c "D:\a\1\s\days-count-go\handlers\polaris.yml" analyze -w'
    waitForIssues: true
  displayName: 'Command Line Script'